from pathlib import Path

name: str = "designName"        # name of your verilog top module
source_path: Path = Path("")    # directory path of the verilog source files (Absolute path is recommended). When not exist, the default path is "mainWorkspace/verilogDesigns"
